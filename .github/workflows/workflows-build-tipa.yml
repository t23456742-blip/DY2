name: Build TIPA

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    tags: ["v*"]

jobs:
  build:
    runs-on: macos-15
    defaults:
      run:
        working-directory: DYSlimClean
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 16
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16"

      - name: Show layout
        run: |
          pwd
          ls -la
          test -f project.yml
          test -f scripts/package-tipa.sh
          test -f Resources/Info.plist

      - name: Show Xcode
        working-directory: .
        run: xcodebuild -version

      - name: Install tools
        working-directory: .
        run: |
          brew install ldid xcodegen
          xcodegen --version

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build (unsigned)
        run: |
          xcodebuild \
            -project DYSlimClean.xcodeproj \
            -scheme DYSlimClean \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            build

      - name: Package tipa
        run: |
          sed -i '' $'s/\r$//' scripts/package-tipa.sh
          chmod +x scripts/package-tipa.sh
          bash scripts/package-tipa.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: DYSlimClean-tipa
          path: |
            DYSlimClean/dist/*.tipa
            DYSlimClean/dist/*.ipa
          if-no-files-found: error
