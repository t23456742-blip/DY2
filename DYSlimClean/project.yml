name: DYSlimClean
options:
  bundleIdPrefix: com.dyslim
  deploymentTarget:
    iOS: "15.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
settings:
  base:
    TARGETED_DEVICE_FAMILY: "1,2"
    IPHONEOS_DEPLOYMENT_TARGET: "15.0"
    SWIFT_VERSION: "5.0"
    CODE_SIGNING_ALLOWED: "NO"
    CODE_SIGN_IDENTITY: ""
    DEVELOPMENT_TEAM: ""
    PRODUCT_BUNDLE_IDENTIFIER: com.dyslim.cleaner
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    GENERATE_INFOPLIST_FILE: "NO"
    INFOPLIST_FILE: DYSlimClean/Resources/Info.plist
    CODE_SIGN_ENTITLEMENTS: DYSlimClean/Resources/DYSlimClean.entitlements
    SUPPORTS_MACCATALYST: "NO"
    ENABLE_BITCODE: "NO"
targets:
  DYSlimClean:
    type: application
    platform: iOS
    sources:
      - path: DYSlimClean
        excludes:
          - Resources/**
      - path: DYSlimClean/Resources
        buildPhase: resources
        excludes:
          - Info.plist
          - DYSlimClean.entitlements
    settings:
      base:
        PRODUCT_NAME: DYSlimClean
        PRODUCT_BUNDLE_IDENTIFIER: com.dyslim.cleaner
        INFOPLIST_FILE: DYSlimClean/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: DYSlimClean/Resources/DYSlimClean.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: ""
        TARGETED_DEVICE_FAMILY: "1,2"
    info:
      path: DYSlimClean/Resources/Info.plist
      properties:
        CFBundleDisplayName: 抖音精简清理
        CFBundleName: 抖音精简清理
        CFBundleDevelopmentRegion: zh-Hans
        CFBundleLocalizations:
          - zh-Hans
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        MinimumOSVersion: "15.0"
schemes:
  DYSlimClean:
    build:
      targets:
        DYSlimClean: all
    run:
      config: Debug
    archive:
      config: Release
