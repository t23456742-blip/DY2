name: DYSlimClean
options:
  bundleIdPrefix: com.dyslim
  deploymentTarget:
    iOS: "15.1"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
settings:
  base:
    TARGETED_DEVICE_FAMILY: "1,2"
    IPHONEOS_DEPLOYMENT_TARGET: "15.1"
    SWIFT_VERSION: "5.0"
    CODE_SIGNING_ALLOWED: "NO"
    CODE_SIGN_IDENTITY: ""
    DEVELOPMENT_TEAM: ""
    PRODUCT_BUNDLE_IDENTIFIER: com.dyslim.cleaner
    MARKETING_VERSION: "15.4"
    CURRENT_PROJECT_VERSION: "154"
    GENERATE_INFOPLIST_FILE: "NO"
    INFOPLIST_FILE: Resources/Info.plist
    CODE_SIGN_ENTITLEMENTS: Resources/DYSlimClean.entitlements
    SUPPORTS_MACCATALYST: "NO"
    ENABLE_BITCODE: "NO"
    OTHER_LDFLAGS: "$(inherited) -lsqlite3 -lz"
targets:
  DYSlimClean:
    type: application
    platform: iOS
    sources:
      - path: .
        excludes:
          - Resources/**
          - scripts/**
          - dist/**
          - build/**
          - "*.yml"
          - "*.md"
          - "*.txt"
          - "*.tipa"
          - "*.ipa"
          - ".github/**"
          - "DYSlimClean/**"
          - "workflows-*"
          - "上传说明*"
          - "workflows-build-tipa.yml"
          - "build-tipa.yml"
      - path: Resources
        buildPhase: resources
        excludes:
          - Info.plist
          - DYSlimClean.entitlements
    settings:
      base:
        PRODUCT_NAME: DYSlimClean
        PRODUCT_BUNDLE_IDENTIFIER: com.dyslim.cleaner
        MARKETING_VERSION: "15.4"
        CURRENT_PROJECT_VERSION: "154"
        INFOPLIST_FILE: Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: Resources/DYSlimClean.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"
    info:
      path: Resources/Info.plist
      properties:
        CFBundleDisplayName: DY助手
        CFBundleName: DY助手
        CFBundleShortVersionString: "15.4"
        CFBundleVersion: "154"
        CFBundleDevelopmentRegion: zh-Hans
        CFBundleLocalizations:
          - zh-Hans
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        MinimumOSVersion: "15.1"
schemes:
  DYSlimClean:
    build:
      targets:
        DYSlimClean: all
    run:
      config: Debug
    archive:
      config: Release

